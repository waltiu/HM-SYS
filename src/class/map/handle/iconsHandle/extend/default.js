import { IconsHandle } from '../index'
import * as maptalks from 'maptalks'
export class DefaultHandle extends IconsHandle {
  constructor (map, layerName = 'defaultHandle') {
    super(map, layerName)
  }

}